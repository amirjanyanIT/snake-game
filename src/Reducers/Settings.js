const initalState = {
    resolution:{
        width:1024,
        height:768
    },
    areaSize:31,
    gameSpeed:50
}


export default (state = initalState, action) => {

    switch(action.type){

        default:
            return state;
    }
}