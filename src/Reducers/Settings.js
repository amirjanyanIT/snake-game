const initalState = {
    resolution:{
        width:1024,
        height:768
    },
    areaSize:17,
    gameSpeed:60
}


export default (state = initalState, action) => {

    switch(action.type){

           default:
            return state;
    }
}