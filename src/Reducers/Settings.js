const initalState = {
    resolution:{
        width:1024,
        height:768
    }
}


export default (state = initalState, action) => {

    switch(action.type){

        default:
            return state;
    }
}